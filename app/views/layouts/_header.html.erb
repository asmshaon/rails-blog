<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container">
    <!--.nav-collapse -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'ShareYouPost', '/', class: 'navbar-brand' %>
    </div>
    <div id="navbar" class="collapse navbar-collapse">

      <% if user_signed_in? %>
          <span class="pull-right user_inout"><a href="/users/sign_out">Logout</a></span>
          <span class="pull-right username">Welcome, <%= link_to current_user.name, edit_user_registration_path %></span>
      <% else %>
          <span class="pull-right user_inout"><a href="#" data-toggle="modal" data-target=".bs-modal-sm">Sign In</a></span>
      <% end %>

      <ul class="nav navbar-nav">
        <% if user_signed_in? %>
            <li><%= link_to 'Share New Post', new_post_path %></li>
        <% end %>
        <li><%= link_to 'About', '/about' %></li>
        <li><%= link_to 'Contact', '/contact' %></li>
      </ul>
    </div>
    <!--/.nav-collapse -->
  </div>
</nav>